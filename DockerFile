# 1단계: 빌드 환경 (Node.js) - React 코드를 빌드하는 역할
FROM node:18-alpine AS builder

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json을 먼저 복사하여 의존성 캐싱 활용
COPY package*.json ./

# 의존성 설치
RUN npm install

# 나머지 소스 코드 전체를 복사
COPY . .

# React 프로젝트를 최적화된 정적 파일(HTML, CSS, JS)로 빌드합니다.
RUN npm run build


# 2단계: 실행 환경 (Nginx) - 빌드된 파일을 서빙하는 역할
FROM nginx:stable-alpine

# 1단계(builder)에서 빌드한 결과물(/app/build 폴더)을
# Nginx 웹 서버의 기본 폴더(/usr/share/nginx/html)로 복사합니다.
COPY --from=builder /app/build /usr/share/nginx/html

# Nginx가 80번 포트를 사용하도록 외부에 알립니다.
EXPOSE 80

# Nginx 서버를 실행합니다.
CMD ["nginx", "-g", "daemon off;"]